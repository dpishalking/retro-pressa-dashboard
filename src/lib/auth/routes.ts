export const HUB_PATH = "/hub";

/** Public UTM link builder — no login required; safe to share with ad contractors. */
export const UTM_GENERATOR_PUBLIC_PATH = "/utm";

/** Public product issue viewer — forever client share links. */
export const PRODUCT_VIEW_PUBLIC_PREFIX = "/view";

/** Public markdown specs / ТЗ — shareable without login. */
export const MD_PUBLIC_PREFIX = "/md";

/** Public product cards gallery — shareable client links. */
export const PRODUCT_CARDS_PUBLIC_PREFIX = "/cards";

/** Authenticated products cabinet (upload / manage share links). */
export const PRODUCTS_PATH = "/products";

/** Partner cabinet (affiliate). */
export const PARTNERS_PATH = "/partners";

/** Public partner self-registration. */
export const PARTNERS_REGISTER_PATH = "/partners/register";

/** Public partner registration API. */
export const PARTNERS_REGISTER_API = "/api/partners/register";

/** Public partner webinar landing. */
export const WEBINAR_PATH = "/webinar";

/** Public catalog of gifts from product passports. */
export const GIFTS_PATH = "/gifts";

/** Public webinar registration API. */
export const WEBINAR_REGISTER_API = "/api/webinar/register";
